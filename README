# code challenge(Delivery route optimization)

### Introduction
This code challenge will solve the optimum route for delivering a package to
different buildings from a source building.

ofcourse, there are some assumptions, on the code challenge, such as the
distance between two buildings are symmetrical

### Installation
download the sourcecode 

### Usage
from the project room directory
you can run with the provided data with

`node index.js`

additionally you can create your own objects from the provided class Building
(`./building.js`) in the index.js and run

`node index.js` 


